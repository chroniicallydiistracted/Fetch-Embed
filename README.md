# Fetch-Embed
An all in one beefy ebook metadata fetching and embedding service designed to query and locate the highest confidence metadata and directly write it into the ebook files current existing.
